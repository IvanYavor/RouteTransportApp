const statusEnum = ["WAITING", "OPERATING", "DONE"];

const transportTypeEnum = ["CAR", "TRUCK"];

const transportStatusEnum = ["AVAILABLE", "UNAVAILABLE"];

const TRANSPORT_AVAILABLE = "AVAILABLE";

module.exports = {
  statusEnum,
  transportStatusEnum,
  transportTypeEnum,
  TRANSPORT_AVAILABLE,
};
